upcoming features (not in order):
1) GUI;
2) Login form:
	a) Better implementation of config.json 
	b) config.json encrypted 
	c) Private Diffie-Hellman half-keys encrypted
	d) Local stored of private Diffie-Hellman half-keys
3) Hash function: SHA-256 
4) Asimmetric encrypted: RSA 2048 / 4096 bits implementations.